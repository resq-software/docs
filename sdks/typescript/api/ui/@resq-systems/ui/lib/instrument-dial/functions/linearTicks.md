# Function: linearTicks()

&gt; **linearTicks**(`min`, `max`, `divisions`): `number`[]

Defined in: [packages/ui/src/lib/instrument-dial.ts:75](https://github.com/resq-software/npm/blob/a23b0e86db5c4539fd4e521f64c284c116e1324a/packages/ui/src/lib/instrument-dial.ts#L75)

Evenly spaced scale values from `min` to `max` inclusive, split into
`divisions` intervals (so `divisions + 1` values are returned).

## Parameters

### min

`number`

### max

`number`

### divisions

`number`

## Returns

`number`[]
