# Function: setEq()

&gt; **setEq**(`a`, `b`): `boolean`

Defined in: [packages/math/src/value.ts:228](https://github.com/resq-software/npm/blob/43e4668edb35f1d8b82814020f177750172b932c/packages/math/src/value.ts#L228)

Structural equality for finite number sets.

## Parameters

### a

`ReadonlySet`\<`number`\>

First set.

### b

`ReadonlySet`\<`number`\>

Second set.

## Returns

`boolean`

`true` when both sets contain exactly the same elements.
