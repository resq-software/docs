# Variable: toPositiveMillis

&gt; `const` **toPositiveMillis**: (`n`) =&gt; [`PositiveMillis`](../type-aliases/PositiveMillis) = `positiveMillis.from`

Defined in: [numeric.ts:128](https://github.com/resq-software/npm/blob/43e4668edb35f1d8b82814020f177750172b932c/packages/types/src/numeric.ts#L128)

Assert `n` is a [PositiveMillis](../type-aliases/PositiveMillis) and return it branded.

## Parameters

### n

`number`

## Returns

[`PositiveMillis`](../type-aliases/PositiveMillis)

## Throws

If `n` is not a finite number strictly greater than zero.
